/// The version shown in Settings → About and on the licenses page. Kept by
/// hand in step with `version:` in pubspec.yaml (a test checks), rather
/// than read at runtime through another plugin.
const appVersion = '1.0.0';
const appBuild = 1;
