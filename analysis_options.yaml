include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - lib/l10n/app_localizations*.dart
    - "**/*.g.dart"
    - build/**
    - android/**
    - ios/**

linter:
  rules:
    # This is an app, not a published package.
    public_member_api_docs: false
