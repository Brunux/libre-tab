<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Shown while the app starts (dark, like the app and the iOS launch
         screen), in light and dark system mode alike. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Android 12+ splash screen: the launcher icon on this color. -->
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
    </style>
    <!-- Behind the Flutter UI while it initializes. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
